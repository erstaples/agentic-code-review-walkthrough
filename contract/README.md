# Request and response fixtures

`fixtures.json` covers representative requests and responses, including invalid
loads and navigation before a tour is loaded. Both MCP and the extension use it.
The maintained TypeScript implementations are in [shared](../shared/README.md).
