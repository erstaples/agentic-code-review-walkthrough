{
  "platform": "macOS arm64",
  "vscode": "1.139.0",
  "baselineCommit": "f6a79e7652dcac48ec44c5192847c62ee6848449",
  "candidateCommit": "db28329",
  "candidateBuildNode": "22.23.3",
  "artifact": "VSIX extracted from clean checkout build",
  "baselinePassed": 30,
  "candidatePassed": 30,
  "checks": [
    "public MCP loads the authored tour and renders a source-backed first beat",
    "public navigation advances beats and stops and can return to an explicit cursor",
    "exploring keeps editors still, paused removes presentation, and following resumes the current beat",
    "the extension rejects invalid load findings before changing the current tour or tabs",
    "stale navigation and retired stop/focus endpoints cannot mutate the tour",
    "copy citation identifies the selected immutable source revision",
    "three active anchors share the cap and removed code falls back to peek",
    "disjoint anchors in one file reuse a tab and present the selected identity",
    "dirty matching head becomes immutable on the next presentation and survives cleanup",
    "pinned tour tabs survive stop changes while ordinary owned previews close",
    "a reviewer-owned preview survives loading and ending a tour",
    "reusing a real file retains the pinned base source for seam peek",
    "clear ends presentation without recording review acceptance, then reload begins at the first beat",
    "layout caps 2, 3 and 4 produce stack, split bottom and grid without duplicating visible anchors",
    "tour pins block all replacement and unpinning makes exactly that group eligible",
    "reviewer resize prevents growth and Exploring leaves both geometry and editors alone",
    "placements split either row, validate caps, and retain remembered role destinations",
    "customized companion groups survive native empty-group cleanup and pins block growth",
    "cramped long sources use Sequence and the one-click override restores multiple groups",
    "stop inventory retains all seven roles and every anchor across beats",
    "explicit move reuses the native tab and reset retains role preferences",
    "moving from an emptying group tolerates native group renumbering",
    "99 source-backed anchors load within the configured ceiling",
    "every required anchor count loads with a complete inventory and bounded tabs",
    "grid placement keeps repeated anchor colors diagonal when there is room",
    "native return and reload restore resized arrangements, pins and remembered roles",
    "closed tabs are reconciled before return and rapid navigation does not accumulate previews",
    "a reviewer tab move is reconciled by native identity and survives return and end",
    "pause closes unclaimed tour tabs and retains dirty reviewer work",
    "a renamed source resolves through its destination path without reviving a saved old anchor"
  ]
}
